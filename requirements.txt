# Core dependencies — the provider-neutral app only. Exact pins for
# supply-chain safety; these are the versions podlink was built and tested
# against. Each cloud's SDK lives in its own requirements-<provider>.txt
# (start.sh installs the active provider's file; CI installs them all), so a
# launch never pulls another cloud's SDK.
#
# Next hardening step: a hash-locked `--require-hashes` lockfile (pip-compile).
fastapi==0.135.1
uvicorn[standard]==0.42.0
httpx==0.28.1

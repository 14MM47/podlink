# Exact pins for supply-chain safety (runpod handles the API key — an
# unexpected release must not slip in). Versions below fastapi/uvicorn/httpx/rich
# are the ones podlink was built and tested against.
#
# NOTE: runpod is pinned to a mature 1.7.x but was not installed in the build
# environment — validate it against your account on first run before relying on
# it. Next hardening step: a hash-locked `--require-hashes` lockfile (pip-compile).
fastapi==0.135.1
uvicorn[standard]==0.42.0
runpod==1.7.13
httpx==0.28.1
rich==14.3.3

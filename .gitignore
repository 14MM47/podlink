# Runtime state written by the pod scripts (never commit — may reference infra)
pod_state.json
template_state.json
logs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Never commit secrets (they live in ~/.config/podlink, not here)
*.key
*token*
*secret*
!pod_control/_secrets.py
